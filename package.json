{
  "name": "codepath-app",
  "version": "1.0.0",
  "description": "CodePath Educational Learning Platform",
  "scripts": {
    "start": "node run-all.js",
    "dev": "node run-all.js",
    "build": "cd client && npm install && npm run build",
    "test": "cd server && node test/api.test.js"
  }
}
